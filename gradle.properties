# Sett UTF-8 eksplisitt for å unngå cache-misser og ulik oppførsel på tvers av OS/JVM-standarder
org.gradle.jvmargs=-Dfile.encoding=UTF-8

# Build cache og configuration cache for raskere lokale builds og CI
org.gradle.caching=true
org.gradle.configuration-cache=true
