package innsending

import org.junit.jupiter.api.Test

class InnsendingTest {
    @Test
    fun `kan sende inn søknad`() {
        assert(true)
    }

    @Test
    fun `kan sende inn vedlegg`() {
        assert(true)
    }
}